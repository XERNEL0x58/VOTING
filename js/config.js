/*
 * Public configuration. The Web App URL is NOT a secret (anyone visiting the site can see it),
 * so it is fine to commit. Never put passwords, tokens or the spreadsheet ID here.
 *
 * Replace the value below with the URL you get from: Apps Script → Deploy → Web app.
 * It looks like: https://script.google.com/macros/s/AKfycb.../exec
 */
window.APP_CONFIG = {
  API_URL: "PASTE_YOUR_WEB_APP_URL_HERE"
};
